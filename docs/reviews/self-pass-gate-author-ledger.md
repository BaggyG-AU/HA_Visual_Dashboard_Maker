Author: Claude Opus (claude-code)
Reviewer: OpenAI Codex
Owner gate: micah/BaggyG-AU

# Author execution ledger — the self-pass gate, rounds 1 and 2

**REGENERATED for this fix round.** Every row below records a check run on
`feature/self-pass-gate` after round 1 of the independent review
(`docs/reviews/self-pass-gate-codex-review.md`, commit `90cc492`) returned
CHANGES-REQUIRED. Nothing is carried over from PR #140, whose ledger was
withdrawn with the rest of the mechanism in `03c817c`.

One row per commissioned check in
`docs/reviews/self-pass-gate-codex-commission.md`.
`tests/support/authorLedger.ts` derives the required row set from that
commission and goes red on a missing row, a duplicate row, an orphan row, a
disposition that is illegal for the row's Kind, a ledger this branch did not
author, or a governed fingerprint that does not match the tree.

⚠⚠ **ROUND 2 RETURNED CHANGES-REQUIRED AND THE OWNER CHOSE TO REDUCE.** Two
mechanisms were CUT rather than repaired and several rows below are therefore
`OWNER-ACCEPTED` — the residue stands, the owner accepted it, and no wording
claims it is closed. What that costs is set out in the commission's
"standing consequence, corrected" section.

## The certificate

⚠⚠ **ROUND 2 CUT TWO OF THE THREE DECLARATIONS.** `base commit` and
`commit-message range` were billed as a base/head lifecycle binding; the
reviewer measured a same-message amend changing the tracked tree with all three
still valid and the gate green. What remains is one declaration with one claim:

**the governed tree — index, working tree and untracked additions — was in this
state when the gate ran.** It says nothing about `src/`, tests, tools, this
ledger's own evidence, or HEAD.

governed fingerprint: `763a4a5efd7d`

## Rows

| ID  | Check                                                        | Disposition    | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | ------------------------------------------------------------ | -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C01 | Commit messages inside the certified scope                   | FIXED          | Branch commit messages are BOTH a checked population and a hashed input. `checkCommitMessageCandidates()` extracts count candidates from `git log --format=%H%n%B base..HEAD` and fails on any not mentioned here, and it fired on this PR's own content commit. ⚠ ROUND 2: the `commit-message range` hash that also covered this surface is CUT, so the checked-population leg is all that remains — and see C47, which measures how weak its substring test has become.                                                                                                                                                                                                                                                                                                                                                    |
| C02 | Fail closed with no resolvable base ref                      | FIXED          | Fixture `the base ref must resolve` deletes `main` on a detached HEAD with no `origin/main` and asserts `loadContext()` throws. Attack-table row `no-base-ref` drives the real spec in a clone with neither ref: NO TESTS RAN, exit 1. Attempt one `it.skip`ped with exit 0.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| C03 | Required rows derived from the commission                    | FIXED          | `checkLedgerCoverage()` builds the required set from `parseCommission()`, never from this file. Control leg: complete ledger GREEN. Known-bad: fixture `a LEDGER row deleted while its commission row remains is red`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| C04 | Renames decomposed and the preimage seen                     | FIXED          | Fixture `a governed file renamed OUT of docs/governance is seen through its preimage` asserts the PREIMAGE is in `ctx.changed` after a committed `git mv`, and that the obligation fires with the commission removed. `--name-only` emits only the destination; `--name-status -M` emits both.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| C05 | Fingerprint sees untracked governed additions                | FIXED          | Fixture `an UNTRACKED governed addition is red` adds `docs/governance/SMUGGLED.md` and asserts the certificate goes red.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| C06 | Live PR-body read mandatory where claimed                    | FIXED          | `bash tools/claims-worklist.sh --require-pr 999999999` exits 2 with an explicit refusal. Measured again this round; the old fail-open path, which swallowed a failed read and returned the local output as though the body had been scanned, is gone.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| C07 | Gate goes RED on a removed commissioned row                  | FIXED          | Fixture `a LEDGER row deleted while its commission row remains is red`, and its converse `an ORPHAN ledger row with no commissioned question is red`. Both drive the real detector against constructed Git state.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| C08 | Ledger regenerated, not copied                               | PASS           | `git log --all --oneline -- docs/reviews/governance-arb-r8-mp-lease-author-ledger.md` finds no such file. Every row here cites a run performed on `feature/self-pass-gate` during this fix round; the round-0 rows were re-measured rather than carried forward.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| C09 | PR body does not claim the M1 class is closed                | PASS           | The append-only observation control (correction 6) is deliberately NOT built, at the owner's direction. The PR body says so explicitly and claims only the repairs actually made.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| C10 | No normative text added to governed paths                    | PASS           | `git diff --name-status -M origin/main...HEAD` plus `git status --porcelain`, both filtered to `docs/governance/`, `docs/templates/`, `ai_rules.md`, `CLAUDE.md`: 0 records from each. A defect here is ordinary PR evidence, not a governance amendment.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| C11 | CI resolves a base ref                                       | PASS           | `grep -rn 'fetch-depth' .github/workflows/` returns exactly `.github/workflows/ci.yml:25`. `grep -rln 'vitest' .github/workflows/` and `grep -rln 'test:unit' .github/workflows/` each return exactly `ci.yml`, so the ONLY workflow that runs this suite is the one checking out full history. Confirmed by a real Actions run on this PR.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| C12 | Fingerprint covers file MODE, not only content               | FIXED          | Fixtures `an UNSTAGED chmod of a tracked governed file is red` and `a STAGED chmod is red too`. The `W` leg records mode from `lstat`, the `I` leg from the index, so both storage states move the hash.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| C13 | A `gh` read failure propagates                               | FIXED          | Best-effort `--with-pr 999999999` exits 0 and says the body was not read; `--require-pr 999999999` exits 2. Decidable SHA checking is delegated to `tools/check-pr-evidence.sh` from PR #138 rather than re-implemented as a grep.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| C14 | RED when the commission FILE is absent                       | FIXED          | Fixture `an ABSENT commission is red, not "nothing owed"`. Old-code measurement in the same checkout: attack-table row `commission-absent` returned exit 0, 7 of 7 passing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| C15 | RED when the `Commissioned checks` heading is missing        | FIXED          | Fixture `a commission whose "Commissioned checks" HEADING is missing is red`. Old code: attack-table row `commission-heading-missing`, exit 0, 7 of 7 passing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| C16 | RED when that section parses to zero rows                    | FIXED          | Fixture `a commission whose table parses to ZERO rows is red`. Old code: attack-table row `commission-table-empty`, exit 0, 7 of 7 passing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| C17 | RED on duplicate commission IDs                              | FIXED          | Fixture `DUPLICATE commission ids are red`, which first asserts `checkLedgerCoverage()` is SATISFIED — the two sets still compare equal — and then that the uniqueness check fails anyway. Old code: attack-table row `commission-duplicate-id`, exit 0, 7 of 7 passing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| C18 | RED when a question is dropped after the reviewer read it    | OWNER-ACCEPTED | owner: BaggyG-AU, 2026-08-10, chose the REDUCE option after round 2 rather than escalate to a whole-tree digest or an external CI attestation. The mechanism this row asks about — the monotonic reviewer anchor — is CUT. Round 2 measured two bypasses: an author-created `docs/reviews/author-self-review.md` becomes the newest snapshot (exit 0, 9 of 9), and a reviewed question can be replaced under a retained ID (exit 0, 9 of 9). Both reproduced independently by the author before the cut. The answer to this question is now NO and nothing claims otherwise; the reasoning is at the cut site in `tests/support/authorLedger.ts`.                                                                                                                                                                             |
| C19 | The pre-review deletion residue is measured and OPEN         | PASS           | Fixture `KNOWN-OPEN: deleting a question together with its ledger row stays GREEN` asserts `runGate()` returns no failures. ⚠ ROUND 2 CORRECTED THIS ROW'S SCOPE: round 1 qualified the residue as "before any reviewer deliverable exists", which implied the anchor closed it afterwards. With the anchor cut the residue is simply open, at any time, and the fixture and its comment now say so. Closing it later breaks that test.                                                                                                                                                                                                                                                                                                                                                                                       |
| C20 | RED when an EMPIRICAL row is DISCLOSED                       | FIXED          | Fixture `an EMPIRICAL row dispositioned DISCLOSED is red`. `DISPOSITION_MATRIX` maps `DISCLOSED` to the empty list, so it passes no kind. Old code: attack-table row `disposition-disclosed`, exit 0, 7 of 7 passing — the test titled "DISCLOSED may not be self-awarded" contained no assertion about DISCLOSED.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| C21 | RED when an EMPIRICAL row is NORMATIVE                       | FIXED          | Fixture `an EMPIRICAL row dispositioned NORMATIVE is red`. Old code: attack-table row `disposition-normative`, exit 0, 7 of 7 passing. Round 1 inferred this one by source inspection; it is now MEASURED.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| C22 | Every disposition exercised against both kinds               | FIXED          | ROUND-2 M3 FOUND THIS ROW WAS A FALSE PASS: it claimed the whole vocabulary was exercised against both kinds while four normative cells — FIXED, OWNER-ACCEPTED, DISCLOSED, UNRUN — had no fixture. Enumerated independently by the author and confirmed. All twelve kind/disposition cells are now fixtures in the `round-1 M2 / round-2 M3` block, plus a guard test asserting the matrix has exactly the six dispositions those fixtures cover, so the claim cannot drift from the tests again.                                                                                                                                                                                                                                                                                                                            |
| C23 | RED on a later branch inheriting both artifacts              | FIXED          | Fixture `a later branch inheriting the ledger unchanged is red`: base set to the certified head, a later commit edits a gate-own file, the ledger is untouched. The certificate passes (the governed tree is unchanged) and `checkLedgerFreshness()` is what fails. ⚠ Its claim is only that the ledger was EDITED here — round 2 measured a future branch passing with inherited evidence, and this check would still accept that.                                                                                                                                                                                                                                                                                                                                                                                           |
| C24 | RED on a later commit with no count candidate                | OWNER-ACCEPTED | owner: BaggyG-AU, 2026-08-10, chose the REDUCE option after round 2 rather than escalate to a whole-tree digest or an external CI attestation. This case was caught by the `commit-message range` hash, which is cut. A later commit whose whole message carries no count-shaped candidate now invalidates nothing. The count-candidate leg still fires on count-shaped claims and is still non-exhaustive, as round 0 disclosed.                                                                                                                                                                                                                                                                                                                                                                                             |
| C25 | A commit-message range hash is feasible                      | PASS           | Still MEASURED as feasible and the finding stands: an amend that preserves the message leaves the range hash byte-identical, measured by the reviewer in round 1 and by the author across three successive amends in the round-1 fix. ⚠ ROUND 2 ESTABLISHED THE NARROWER AND DECISIVE POINT: feasible is not sufficient. A hash deliberately stable across an amend is deliberately blind to what that amend changed, so it can never be a tree certificate. It is not used.                                                                                                                                                                                                                                                                                                                                                  |
| C26 | The false rationale corrected everywhere it appears          | FIXED          | A three-term sweep over `*.ts`, `*.md`, `*.sh` and `*.yml` outside `node_modules`, keyed separately on `infinite regress`, on `is impossible` and on `cannot work`: every surviving hit is either a CORRECTION of the claim, the reviewer's own immutable document, or unrelated text elsewhere in the repository. The only two places that ASSERTED it were the spec docblock and row C01 of this ledger, and both are rewritten.                                                                                                                                                                                                                                                                                                                                                                                            |
| C27 | Fingerprint sees an UNSTAGED content edit                    | FIXED          | Fixture `an UNSTAGED content edit to a tracked governed file is red`. Old code: attack-table row `unstaged-content-edit`, exit 0, 7 of 7 passing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| C28 | Fingerprint sees an UNSTAGED deletion                        | FIXED          | Fixture `an UNSTAGED deletion of a tracked governed file is red`; the `W` leg records `absent`. Old code: attack-table row `unstaged-deletion`, exit 0, 7 of 7 passing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| C29 | Fingerprint sees an UNSTAGED mode change                     | FIXED          | Fixture `an UNSTAGED chmod of a tracked governed file is red`. No setup in that block calls `git add`. Old code: attack-table row `unstaged-chmod`, exit 0, 7 of 7 passing — round 1's measured false accept.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| C30 | The STAGED mode control still holds                          | PASS           | Fixture `a STAGED chmod is red too`. Old code already caught this one: attack-table row `staged-chmod`, exit 1. It is kept as a regression control because it is the narrower property the author had mistaken for the wider one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| C31 | Fingerprint sees a file replaced by a SYMLINK                | FIXED          | Fixture `replacing a tracked governed file with a SYMLINK is red`. `worktreeFacts()` records type from `lstat` and hashes the link target, so a symlink and a file with identical bytes are distinguishable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| C32 | Generator exits non-zero on an unrelated base                | FIXED          | Constructed a parentless root with `commit-tree` so `unrelated` RESOLVES but shares no ancestor with HEAD. `HAVDM_BASE_REF=unrelated bash tools/claims-worklist.sh --changed-only`: OLD exit 0 with zero lines of output, NEW exit 2. Controls unchanged: missing ref exits 2, ordinary related base exits 0 with 6 paths.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| C33 | Generator emits both sides of a STAGED rename                | FIXED          | Staged `git mv docs/governance/OPERATING_AGREEMENT.md docs/reviews/moved-operating-agreement.md`, then `--changed-only`: OLD preimage 0 destination 1, NEW preimage 1 destination 1. The unstaged filesystem-move control was already 1 and 1 in both.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| C34 | A pathname containing a newline stays ONE record             | FIXED          | An untracked `docs/governance/we<newline>ird.md` against the banner's own record count: baseline 6 paths, OLD 8 paths, NEW 7 paths. ⚠ The `--changed-only` DISPLAY is newline-framed, so this had to be measured from the record count rather than from that output.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| C35 | Each row is one falsifiable question                         | FIXED          | Round 1 named C02, C05 and C06 as compounds. Each is NARROWED to its first clause and its second clause is now C11, C12 and C13 respectively; no ID was renumbered or dropped. ⚠ ROUND 2: the check that enforced that is CUT, so this is now a discipline rather than a mechanism, and round 2 additionally measured that a question can be REPLACED under a retained ID — see C45. Atomicity of CONTENT remains a hand check.                                                                                                                                                                                                                                                                                                                                                                                               |
| C36 | The live PR body is outside the blocking path                | PASS           | `grep -rn 'claims-worklist' tests/` returns one COMMENT in `tests/support/authorLedger.ts` and no invocation, so a false claim present only in the live PR body does NOT turn the gate red. The boundary is stated in the commission and in the detector's header docblock. Whether to bring it into the blocking path is an owner decision; round 1 classified it non-blocking.                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| C37 | Every round-1 false accept now goes RED                      | FIXED          | The reproduced table below carries round 1, the round-1 fix and the reduction side by side. All five of round 1's tabulated false accepts remain red after the reduction. ⚠ THREE STATES THAT WERE RED AFTER ROUND 1 ARE GREEN AGAIN, each for a stated reason, and none is hidden: two follow from the cuts the owner chose, and one is a stale fixture.                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| C38 | The hostile cases are re-run, not recorded                   | FIXED          | They are `tests/unit/author-ledger-fixtures.spec.ts`, inside `npm run test:unit` and therefore inside `./tools/checks` and CI. They build real Git state in a temporary repository and drive the same functions the blocking gate drives, so neither file re-implements the other's parser.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| C40 | N1's class swept in the TypeScript detector too              | FIXED          | A finding is a sample, not the population: N1 was reported against the shell script, and the same swallow was in the detector. `grep -n 'tryGit(' tests/support/authorLedger.ts` now returns four lines — the definition, the base-ref probe where a failed resolve IS the question, the `git show` of the commission at the reviewer's commit where absence is a real answer, and a comment. The two calls that DEFINED a population, the commit-message log and the reviewer-deliverable log, now use the throwing `git()`.                                                                                                                                                                                                                                                                                                 |
| C39 | The agent never merges                                       | NORMATIVE      | Quoted from `docs/governance/OPERATING_AGREEMENT.md` §3 class (d) as ratified by PR #139: one full independent review before merge, NO automatic follow-up round, and the OWNER decides whether a post-review change warrants re-review. This row exists so the kind and disposition matrix is exercised on this branch and not only in fixtures.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| C41 | Obligation scoped to governed and gate-own paths             | FIXED          | Fixture `an ordinary later branch touching neither governed text nor this gate owes nothing` asserts `hasBranchWork` true, `changed` exactly one out-of-scope path, `owesLedger` FALSE and the whole gate green — with an inherited commission and ledger still sitting in the tree. Round 2 called the previous condition, any branch work at all, "a policy mechanism introduced through an ordinary unit test" affecting every contributor and dependency bump in the repository.                                                                                                                                                                                                                                                                                                                                          |
| C42 | Deleting the artifacts does not escape the obligation        | FIXED          | Fixture `deleting the commission and ledger does NOT escape the obligation`: both paths are in `GATE_OWN`, so removing them is itself a change to this gate. `owesLedger` stays true and `checkCommissionInput()` fails. Without this the scoping in C41 would be a hole rather than a scope.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| C43 | No surviving text claims a tree, HEAD or freshness binding   | FIXED          | Swept the four surfaces separately — detector, commission, this ledger, and the live PR body — for the words the round-1 fix used: base/head lifecycle, "every future branch owes its own commission and its own regenerated ledger", and any claim of certifying branch content. Every one is rewritten or deleted. The certificate section above now states the single claim and names four things it excludes. Round 2 measured the old wording false, so this is a correction of record, not a style pass.                                                                                                                                                                                                                                                                                                                |
| C44 | Each cut is documented at its cut site with the measurement  | FIXED          | Both cut sites in `tests/support/authorLedger.ts` carry a block naming the mechanism, the measured bypass with its exit code, and an instruction not to re-add it. The anchor site ends "DO NOT RE-ADD A FILENAME, AUTHOR-NAME OR COMMIT-ORDER HEURISTIC HERE", quoting round 2's own direction. Round 1 lost a day rebuilding reasoning that had not been written down at the site.                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| C45 | A reviewed question can be replaced under a retained ID      | OWNER-ACCEPTED | owner: BaggyG-AU, 2026-08-10, chose the REDUCE option after round 2 rather than escalate to a whole-tree digest or an external CI attestation. MEASURED, and reproduced independently by the author before the cut: replacing reviewed C02's question text with "Does two plus two equal four?" under the same ID and Kind left the real gate at exit 0, 9 of 9. Hashing question text instead would forbid the legitimate narrowing this branch performed on C02, C05 and C06, so there is no smaller mechanism that closes it. The protection is the visible diff and the human reviewer.                                                                                                                                                                                                                                   |
| C46 | A same-message amend can change tracked non-governed content | OWNER-ACCEPTED | owner: BaggyG-AU, 2026-08-10, chose the REDUCE option after round 2 rather than escalate to a whole-tree digest or an external CI attestation. MEASURED by round 2 and reproduced by the author from a clean certified head: staging a line into `tools/claims-worklist.sh` and running the prescribed `git commit --amend --no-edit` moved HEAD and the TREE while the gate returned exit 0, 9 of 9. The certificate now claims only the governed tree, so nothing is falsely certified — but `src/`, tests, tools, the commission text and this ledger's evidence are outside it. A whole-tree digest or an external CI attestation would close this class.                                                                                                                                                                 |
| C47 | The count-candidate leg matches a substring, not a row       | OWNER-ACCEPTED | owner: BaggyG-AU, 2026-08-10, chose the REDUCE option after round 2 rather than escalate to a whole-tree digest or an external CI attestation. MEASURED, and this is a residue the reduction REVEALED rather than created: the attack-table fixture that appends a `777 tests passed` commit now returns exit 0, because this ledger's own reproduced table contains the literal string `777 tests` and the check accepts a candidate found ANYWHERE in the file. Round 0 disclosed the substring behaviour and round 1 restated it; what is new is that a growing ledger auto-satisfies more candidates. Under the old code this fixture was red for an unrelated reason — the base-commit mismatch — which masked it. Parsing the candidates table instead is a real fix and is deliberately not made in a reduction round. |

## The attack table, reproduced across all three states

Driven through the REAL blocking spec from the repository's installed Vitest
with `HAVDM_LEDGER_REPO` pointed at isolated shared clones. "R1" is round 1's
published result, "fix" is the round-1 fix round, "reduced" is this round.

| Constructed state                                           | R1       | fix | reduced | Note                                               |
| ----------------------------------------------------------- | -------- | --- | ------- | -------------------------------------------------- |
| Checked-in branch head, unmutated                           | 0        | 0   | **0**   | green control holds                                |
| Reflog first content commit `faf55d8`                       | 1        | 1   | **1**   |                                                    |
| Commission file absent                                      | 0 accept | 1   | **1**   |                                                    |
| Commission heading renamed                                  | —        | 1   | **1**   |                                                    |
| Commission table emptied                                    | —        | 1   | **1**   |                                                    |
| Duplicate commission id, ledger row removed                 | 0 accept | 1   | **1**   |                                                    |
| Ledger row absent, commission row retained                  | 1        | 1   | **1**   |                                                    |
| `C01` = `DISCLOSED`                                         | 0 accept | 1   | **1**   |                                                    |
| `C01` = `NORMATIVE`                                         | —        | 1   | **1**   |                                                    |
| Unstaged content edit / deletion / chmod of a governed file | 0 accept | 1   | **1**   | three states, all three red                        |
| Staged chmod                                                | 1        | 1   | **1**   |                                                    |
| Governed fingerprint = `not-a-hash`                         | 1        | 1   | **1**   |                                                    |
| Neither `main` nor `origin/main`                            | 1        | 1   | **1**   | suite load failure                                 |
| Commission row AND its ledger row deleted                   | 0 accept | 1   | **0**   | ⚠ the anchor was cut — now KNOWN-OPEN, see C18/C19 |
| Simulated merged main, artifacts inherited                  | 0 accept | 1   | **0**   | ⚠ FIXTURE IS STALE — see below                     |
| Later `777 tests passed` commit                             | 1        | 1   | **0**   | ⚠ substring residue revealed — see C47             |

**All five of round 1's tabulated false accepts remain red.** Three states that
were red after the fix round are green again, and none of the three is hidden:

1. **Commission row + ledger row deleted.** The monotonic anchor was cut on the
   owner's instruction after round 2 measured two bypasses. This is now the
   `KNOWN-OPEN:` fixture, asserted GREEN on purpose.
2. **Simulated merged main.** ⚠ **THIS FIXTURE NO LONGER MODELS WHAT IT SAYS.**
   It sets the base to `2bd1aa7`, so relative to that base the ledger WAS
   authored on the branch and `checkLedgerFreshness()` correctly passes. The
   real inheritance case — base at the certified head, later work, ledger
   untouched — is the in-suite fixture `a later branch inheriting the ledger
unchanged is red`, and it is RED. The external fixture is retired rather
   than reinterpreted.
3. **Later `777 tests passed`.** Green because this ledger's own table above
   contains the literal string `777 tests`, and the check accepts a candidate
   found anywhere in the file. Round 0 disclosed the substring behaviour; the
   base-commit binding had been masking it. Recorded as C47.

## Commit-message claim candidates (the M1 leg)

The count key over `git log --format=%H%n%B origin/main..HEAD` returns eight
candidates across this branch's four commits — the content commit, the round-1
review, the round-1 fix and this reduction. All eight are dispositioned, which
is what the M1 leg requires before the gate goes green.

⚠ See C47: this leg accepts a candidate found ANYWHERE in this file, so the
table below is weaker evidence than it looks. It is kept because it is what the
check actually tests, and its weakness is recorded rather than papered over.

| Candidate      | Disposition | Evidence                                                                                                                                                                                                                        |
| -------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `1342 tests`   | PASS        | `./tools/checks` at `2bd1aa7`: 1342 passed. The branch total before round 1's fix.                                                                                                                                              |
| `102 files`    | PASS        | Same run: 102 test files, up from 101 on `main`.                                                                                                                                                                                |
| `1378 tests`   | PASS        | `./tools/checks` at `99a3080`, the round-1 fix: 1378 passed.                                                                                                                                                                    |
| `103 files`    | PASS        | Same run: 103 test files. The added file is `tests/unit/author-ledger-fixtures.spec.ts`.                                                                                                                                        |
| `1384 tests`   | PASS        | `./tools/checks` on this reduction: 1384 passed. The six added since `99a3080` are the four missing normative disposition cells, the matrix guard, and the scoping pair, less the fixtures deleted with the two cut mechanisms. |
| `103 files`    | PASS        | Same run: unchanged at 103. The reduction adds no file and removes none.                                                                                                                                                        |
| `0 errors`     | PASS        | Same run: `✖ 145 problems (0 errors, 145 warnings)`.                                                                                                                                                                            |
| `145 warnings` | PASS        | Same run, unchanged from the `9e95e4c` baseline, so this branch still adds no lint debt.                                                                                                                                        |

## `tools/check-pr-evidence.sh` against the LIVE PR body (the PR #138 DoD item)

Run against the live body of PR #141 — not a local copy — it exits 0. Only
section 3 decides anything.

### Section 3 — commit SHAs in the body, the decidable check

Five SHAs, **all ancestors of HEAD**, all deliberate immutable pins: the four
commits on this branch (`2bd1aa7` content, `90cc492` round 1, `99a3080` the
round-1 fix, `3343530` round 2) and `9e95e4c` the merge base. **No head SHA is
pinned anywhere in the body.** Round 1's version of this section had to be
corrected after the tool flagged a quoted pre-amend object that no one else
could resolve; that lesson holds.

### Sections 1 and 2 — counts and universals in the live body

| Candidate                                                        | Justification                                                                                                                                                              |
| ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `0 errors`, `145 warnings`, `1384 passed`, `103 files`           | MEASURED in the `./tools/checks` run reported in the body, and dispositioned row by row in the commit-message table above.                                                 |
| "claiming **every** disposition was exercised"                   | A quotation of the FALSE claim round 2 refuted, not a fresh claim. The true enumeration — twelve cells, four absent — is row C22.                                          |
| "two of the three were answered by **withdrawing** a claim"      | Enumerated: M1 cut, M2 cut, M3 fixed. Three findings, named individually in the table directly above the sentence.                                                         |
| "its claim is **only** that the ledger was edited"               | A deliberate NARROWING of a claim. Weakening statements need no enumeration; the fixture that bounds it is named in row C23.                                               |
| "the obligation is scoped … an ordinary branch owes **nothing**" | MEASURED by the fixture `an ordinary later branch touching neither governed text nor this gate owes nothing`, which asserts `owesLedger` false and the gate green.         |
| "**no** amend loop, **no** recertification cycle"                | MEASURED by absence: `grep -n 'messageRangeHash\|base commit' tests/support/authorLedger.ts` returns only the two cut-site docblocks, no code.                             |
| "**every** future branch would owe … was FALSE"                  | Round 2's measurement, reproduced by the author: a future branch passed with inherited evidence after two line edits. Row C46 and the corrected section in the commission. |
| "the certificate covers the governed tree **only**"              | MEASURED: `GOVERNED` is four path regexes and `governedFingerprint()` filters every leg through `isGoverned()`. The exclusions are named in the same sentence.             |
| "a false claim present **only** in the live PR body"             | MEASURED: `grep -rn 'claims-worklist' tests/` returns one comment and no invocation.                                                                                       |

### The whole-file noise, named rather than left to the reviewer

The script scans WHOLE changed files, not diff hunks. The files it flags are
this mechanism's own artifacts, where the numerals ARE the measurements, plus
the two REVIEWERS' deliverables (`self-pass-gate-codex-review.md` and
`self-pass-gate-codex-round2-review.md`), whose counts are the reviewer's own
and are not the author's to edit.

### The watched flake fired once, and was discriminated rather than re-run

`tests/unit/DeployDialog.spec.tsx` timed out at 5000 ms in one full-suite run
of `./tools/checks` during this round. Per the documented runbook it was
DISCRIMINATED, not re-run: the working-tree change was stashed and the spec run
alone against clean `HEAD` in the same checkout (**11 of 11**), then restored
and run alone again (**11 of 11**). The change in flight was markdown-only and
cannot reach that component. Nothing was rebaselined and the spec was not
touched. The subsequent full gate returned REAL_EXIT=0, 4/4, 1384 of 1384.

## What this ledger does not establish

- **It cannot see a question that was never written**, and — before a reviewer
  deliverable exists on the branch — it cannot see one that was deleted either.
  That residue is row C19, and it is pinned by a test that asserts the gate
  stays GREEN rather than by a sentence.
- **Row atomicity is enforced as one unique ID per row.** Whether a row's
  sentence is one question is a hand check; three rows failed it in round 1.
- **The disposition matrix constrains the LABEL, not the sentence.** Nothing
  stops a broad or evasive assertion in an evidence cell.
- **The lifecycle binding stops INHERITANCE and STALENESS, not a determined
  history rewrite.** An author who rewrites the branch can regenerate a
  self-consistent certificate.
- **The monotonic anchor is GONE.** Round 1 disclosed that its filename glob
  was a weak key; round 2 measured that it was not a key at all, because the
  author can create a matching file. Deleting or replacing a commissioned
  question is now open at any time — see C18, C19 and C45.
- **A reviewer's commit no longer turns the gate red.** That behaviour came
  from the `commit-message range` hash, which is cut. There is no
  recertification cycle and no amend loop.
- **The certificate does not bind branch content.** A same-message amend can
  change `src/`, tests, tools, the commission or this ledger's evidence with
  nothing invalidated — measured by round 2 and reproduced by the author. See
  C46.
- **`HAVDM_BASE_REF` is a test hook and therefore an escape hatch.** Setting it
  to HEAD makes `hasBranchWork` false and skips the commission checks. CI does
  not set it, and an author who did could equally delete the spec — but it is
  named here rather than left for the reviewer, because it bends this
  mechanism's own "an unknown population must never present as an empty one".
- **`fetch-depth: 0` and `--no-merges` are verified on this repository's own CI
  run**, not on forks and not across a matrix of Git versions.
- **The append-only observation control is not built**, so the PR #140 M1 class
  is not addressed and is not claimed to be.
- **E2E, integration, UAT, packaging and live Home Assistant were not run.** No
  `src/` path changed, so none is owed; they remain UNVERIFIED since PR #128,
  which is a statement about the suites, not about this branch.
