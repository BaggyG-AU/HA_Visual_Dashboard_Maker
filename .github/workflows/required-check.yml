name: Required Check

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  required-check:
    name: required-check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "required-check ok"
