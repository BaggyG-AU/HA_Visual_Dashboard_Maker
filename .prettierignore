node_modules/
out/
dist/
.vite/
coverage/
test-results/
package-lock.json

# Playwright snapshot fixtures — byte-compared, must not be reformatted.
**/*-snapshots/
**/*.snap

# Test fixtures are DATA, not source. Several tests assert on exact YAML/JSON
# text (round-trip fidelity, legacy-format migration), so reformatting them
# would silently change what those tests verify.
tests/fixtures/

# UAT round artifacts are produced by the TESTER, not by us, and
# `docs/testing/UAT_STRATEGY.md` §12 requires them committed verbatim. The
# session JSON is the input to the next round's previous-round dots (§8), so
# rewriting it would rewrite the evidence a regression is detected against.
# The matrices are NOT listed here — those are ours, and are deliberately
# formatted before `npm run verify:uat-matrix` re-checks the committed file.
docs/testing/uat/sessions/
docs/testing/uat/reports/
docs/testing/uat/archives/

# Generated / vendored
*.min.js
*.min.css
